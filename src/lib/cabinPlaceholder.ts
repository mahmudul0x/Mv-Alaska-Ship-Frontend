/** Fallback card/hero image for cabins whose gallery hasn't been uploaded
 * from the staff dashboard yet — the page never renders a broken frame. */
import placeholder from "@/assets/103.jpeg";

export const cabinPlaceholder = placeholder;
